default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  # Kept safely above Puma's web thread count (RAILS_MAX_THREADS, default
  # 10) so background threads that briefly need a connection can't starve
  # web requests. Also covers Sidekiq (concurrency 5) in its own process.
  pool: 22
  host: localhost
  password: <%= ENV['PORTFOLIO_DB_PASS'] %>

development:
  <<: *default
  database: Portfolio_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: Portfolio_test

production:
  <<: *default
  database: Portfolio_production
  username: rails
  password: <%= ENV['PORTFOLIO_DB_PASS'] %>
