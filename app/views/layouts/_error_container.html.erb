<% if resource.try(:errors).try(:any?) %>
  <div class="error-container">
    <ul>
      <% resource.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>
