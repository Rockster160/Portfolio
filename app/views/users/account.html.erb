<div class="skinny-container">

  <div class="app-card-container">
    <div class="app-card-body">
      <div class="app-card-inner">
        <div class="list-container no-padding">
          <div class="list-items">
            <%= link_to lists_path, class: "list-item" do  %>
              <span class="item-name">Lists</span>
            <% end %>
            <%= link_to recipes_path, class: "list-item" do  %>
              <span class="item-name">Recipes</span>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <%= form_for @user do |f| %>
    <%= hidden_field_tag :list_id, params[:list_id] %>
    <div class="form-title">Account</div>

    <%= render partial: 'layouts/error_container', locals: { resource: @user } %>

    <div class="form-fields">
      <div class="form-field">
        <%= f.label :username %>
        <%= f.text_field :username %>
      </div>
      <div class="form-field">
        <%= f.label :email %>
        <%= f.text_field :email %>
      </div>
      <div class="form-field">
        <%= f.label :phone %>
        <%= f.telephone_field :phone %>
      </div>

      <div class="form-field text-center">
        <div class="checkbox-wrapper">
          <%= f.check_box :dark_mode %>
          <%= f.label :dark_mode do %>
            <div class="checkbox-placeholder"></div>
            Use dark mode?
          <% end %>
        </div>
      </div>

      <div class="change-password-fields" style="margin-top: 20px;">
        <p class="text-center">Use the fields below to change your password.</p>
        <div class="form-field">
          <%= f.label :current_password, "Current password *" %>
          <%= f.password_field :current_password %>
        </div>
        <div class="form-field">
          <%= f.label :password %>
          <%= f.password_field :password %>
        </div>
        <div class="form-field">
          <%= f.label :password_confirmation %>
          <%= f.password_field :password_confirmation %>
        </div>
      </div>
    </div>

    <div class="form-fields">
      <div class="form-field">
        <%= f.submit "Update", class: "wide" %>
      </div>
    </div>
  <% end %>

  <!-- <div class="app-card-container">
    <div class="app-card-body">
      <div class="app-card-inner">
        <div class="list-container no-padding">



        </div>
      </div>
    </div>
  </div> -->
</div>
