<% display = clean_md(item[:logo] || item[:name]) %>

<div class="widget-holder">
  <div class="delete-widget hidden">❌</div>
  <div class="widget mini-widget" data-logo="<%= item[:logo] %>" data-name="<%= item[:name] %>" data-page="<%= item[:page] %>" data-command="<%= item[:command] %>">
    <% if display.match?(/^ti-/) %>
      <i class="emoji ti <%= display %>"></i>
    <% elsif display.match?(/[\p{So}\p{Sk}\p{Sm}\p{Sc}\p{S}\p{C}]/) && !display.match?("<i") %>
      <i class="emoji"><%= display.html_safe %></i>
    <% else %>
      <%= mrkdwn(display) %>
    <% end %>
    <% if item[:name].present? && item[:name] != display %>
      <span class="title"><%= item[:name] %></span>
    <% end %>
  </div>
</div>
