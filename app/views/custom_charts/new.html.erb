<%= content_for(:title) { "New chart" } %>
<%= render "chart_assets" %>

<div class="cc-index-head" style="max-width: 900px; margin: 0 auto; padding: 12px 12px 0;">
  <div class="cc-title"><%= link_to "Charts", custom_charts_path, class: "cc-crumb" %> / New</div>
</div>

<%= render "form" %>
